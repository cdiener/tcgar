box: rocker/hadleyverse

build:
    steps:
        - script:
            name: install package
            code: Rscript -e "devtools::install()"
        - script:
            name: test package
            code: Rscript -e "devtools::test()"
            
